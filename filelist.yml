    ---
    - name: Example Playbook using Survey Variable
      hosts: all
      tasks:
        - name: Display the survey variable
          debug:
            msg: "The value from the survey is: {{ survey_variable_name }}"